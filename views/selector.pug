extends layout

block content
  h1 Selector
  - var image_names = ["1.jfif", "2.jfif", "3.jpg", "4.png", "5.jpg","6.jpg"]
  
  - for(var i=0;i<3;i++){      
    - var random = Math.floor(Math.random()*5);
      img(src="/images/"+image_names[random] , height="400px", width="400px", style="margin:10px")

  - }
  script.
  console.log(images[random])
  

  

